{
  "name": "word-hierarchy-analizer",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^22.7.9"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "analyze": "bun run cli.ts analyze -–depth 3 --verbose true 'Eu vi gorilas e papagaios'",
    "build": "bun --target=bun build cli.ts --outdir ./build",
    "start": "bun run build/cli.js"
  }
}
